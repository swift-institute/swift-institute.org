---
name: code-organization
description: |
  SUPERSEDED by **code-surface** skill.
  All API-IMPL-* rules are now in the code-surface skill.
  This skill remains for backwards compatibility only.

layer: implementation

superseded_by: code-surface

requires:
  - swift-institute

applies_to:
  - swift
  - swift6
  - primitives
  - standards
  - foundations
---

# Code Organization Conventions (SUPERSEDED)

**This skill has been superseded by the `code-surface` skill.** All rules — [API-IMPL-003], [API-IMPL-005] through [API-IMPL-008] — have been absorbed into that skill.

Use `/code-surface` instead of `/code-organization`.
