---
name: errors
description: |
  SUPERSEDED by **code-surface** skill.
  All API-ERR-* rules are now in the code-surface skill.
  This skill remains for backwards compatibility only.

layer: implementation

superseded_by: code-surface

requires:
  - swift-institute

applies_to:
  - swift
  - swift6
  - primitives
  - standards
  - foundations
---

# Error Handling Conventions (SUPERSEDED)

**This skill has been superseded by the `code-surface` skill.** All rules — [API-ERR-001] through [API-ERR-005] — have been absorbed into that skill.

Use `/code-surface` instead of `/errors`.
